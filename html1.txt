<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Guess The Number Game</title>

<style>
*{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family:Arial, Helvetica, sans-serif;
}

body{
    background:linear-gradient(135deg,#141E30,#243B55);
    display:flex;
    justify-content:center;
    align-items:center;
    height:100vh;
}

.container{
    width:400px;
    background:white;
    padding:30px;
    border-radius:15px;
    text-align:center;
    box-shadow:0 15px 40px rgba(0,0,0,.3);
}

h1{
    color:#333;
    margin-bottom:15px;
}

p{
    color:#666;
    margin-bottom:20px;
}

input{
    width:100%;
    padding:12px;
    font-size:18px;
    border:2px solid #ddd;
    border-radius:8px;
    outline:none;
}

input:focus{
    border-color:#007bff;
}

button{
    width:100%;
    padding:12px;
    margin-top:15px;
    border:none;
    border-radius:8px;
    background:#007bff;
    color:white;
    font-size:18px;
    cursor:pointer;
}

button:hover{
    background:#0056b3;
}

#message{
    margin-top:20px;
    font-size:22px;
    font-weight:bold;
}

.score{
    margin-top:20px;
    display:flex;
    justify-content:space-between;
    font-size:18px;
    color:#333;
}
</style>

</head>

<body>

<div class="container">

<h1>🎮 Guess The Number</h1>

<p>Guess a number between <b>1 and 100</b></p>

<input
type="number"
id="guess"
placeholder="Enter your guess"
min="1"
max="100">

<button onclick="checkGuess()">
Guess
</button>

<div id="message"></div>

<div class="score">
<div>Attempts: <span id="attempts">0</span></div>
<div>Wins: <span id="wins">0</span></div>
</div>

</div>

<script>

/* ==========================
   BACKEND (Game Logic)
   ========================== */

let secretNumber = Math.floor(Math.random()*100)+1;

let attempts = 0;
let wins = 0;

function newGame(){

    secretNumber = Math.floor(Math.random()*100)+1;

    attempts = 0;

    document.getElementById("attempts").innerHTML = attempts;

    document.getElementById("guess").value = "";

    document.getElementById("guess").focus();

}


/* ==========================
   FRONTEND
   ========================== */

function checkGuess(){

    let input = document.getElementById("guess");

    let guess = Number(input.value);

    let message = document.getElementById("message");

    if(guess<1 || guess>100 || isNaN(guess))
    {
        message.style.color="red";
        message.innerHTML="⚠ Please enter a number between 1 and 100";
        return;
    }

    attempts++;

    document.getElementById("attempts").innerHTML=attempts;

    if(guess>secretNumber)
    {
        message.style.color="orange";
        message.innerHTML="📈 Too High!";
    }

    else if(guess<secretNumber)
    {
        message.style.color="blue";
        message.innerHTML="📉 Too Low!";
    }

    else
    {
        wins++;

        document.getElementById("wins").innerHTML=wins;

        message.style.color="green";

        message.innerHTML="🎉 Congratulations! You guessed it correctly.<br><br>Starting New Game...";

        setTimeout(function(){

            newGame();

            message.style.color="green";

            message.innerHTML="✅ New Game Started! Guess the new number.";

        },2500);
    }

    input.value="";
    input.focus();

}

document.getElementById("guess").addEventListener("keypress",function(event){

    if(event.key==="Enter")
    {
        checkGuess();
    }

});

</script>

</body>
</html>